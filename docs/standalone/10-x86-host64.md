# Fase 10 — x86 carrier + Host64

## Objetivo

Permitir execução x64 para jogos 32-bit decompondo transporte e processamento por ownership.

## Dependências

Fases 03 e 07.

## Fora de escopo

- Runtime completo x86
- Pixels no pipe
- D3D11 x86 como prova de todas APIs

## Implementação

- [x] Reusar CaptureProvider32/HostServer64/tests.
- [x] Split `HostServer64.cpp`: **D3D12/shared-resource context**, **session/server lifecycle**, **frame processing**.
- [x] Split `CaptureProvider32.cpp`: **connection/config/handle duplication** e **pipelined frame transport/acks**.
- [x] Capture hook específico da API fica fora do IPC comum.
- [x] Preservar DummyCopy como prova barata.
- [x] Versionar IPC/session generation.
- [x] Transportar só control + handles + fences.
- [x] Tratar disconnect/crash/exit/stale.
- [x] Policy/model só Host64.

## Revisão obrigatória

- [x] Cada HANDLE creator/target/consumer/close.
- [x] Reconnect não reutiliza generation.
- [x] Sem wait infinito.
- [x] IPC não carrega pixels.
- [x] Splits não duplicam protocolo.

## Validação rápida

- [x] Handshake/pipeline/DummyCopy.
- [x] Win32→x64 real.
- [x] Stress disconnect/stale.
- [x] LOC checker.

## Gate

- [x] Transporte/lifecycle sem modelo.
- [x] Sem leaks.
- [x] Host/carrier <=300 por arquivo.

## Próxima fase

Fase 11.

## Subgates concluídos

### 10a — split do client IPC

- `CaptureProvider32.cpp` foi separado por ownership.
- conexão/configuração/duplicação de HANDLEs: 255 linhas;
- pipeline/acks N/N-1: `CaptureProvider32Frames.cpp`, 132 linhas;
- `nrfusion_capture32` entrou no Windows fast gate.

### 10b — generation e no-wait

- protocolo IPC passou para v3 com `connectionGeneration` explícita;
- Build/Frame/Ack validam generation + session em conjunto;
- Host64 incrementa generation a cada nova conexão;
- `HostServer64Guides` não faz mais `WaitForSingleObject(..., INFINITE)`;
- resize de guides recusa reutilização enquanto o fence anterior não aposentou;
- Windows fast compila um Host64 IPC slice sem linkar o core inteiro;
- portable e Windows hosted: PASS no commit `de62b05`.

## Próximo subgate

- tornar o host fence um marcador real de retirement: consumer fence deve ser sinalizado antes do host fence;
- manter resources/fences importados vivos após disconnect até o host fence aposentar;
- separar esse ownership em um owner de transporte Host64, sem crescer `HostServer64Frame.cpp`;
- depois eliminar waits não limitados restantes no handshake/config antes do gate Win32→x64.


## Fechamento — review fixes

Fase 10: **CLOSED**.

Correções derivadas da revisão:
- consumer fence agora é sinalizado antes do host retirement fence;
- resources/fences importados sobrevivem ao disconnect até o host fence aposentar;
- zero guides respeitam o último frame que os consumiu, não só o upload;
- command list é fechado em toda saída depois de Reset bem-sucedido;
- setup IPC usa timeout limitado e cancelamento OVERLAPPED não é reutilizado antes de aposentar;
- short read/write de frame falha fechado e invalida o transporte;
- Build v3 stale/invalid fecha HANDLEs duplicados recebidos;
- connectionGeneration permanece única através de restart do processo Host64;
- CaptureProvider32 foi dividido em connection/config, IO lifecycle e frame pipeline, todos <=300 linhas.

Validação:
- portable CI: PASS;
- Windows hosted fast CI: PASS;
- Win32 capture -> x64 Host64 físico: PASS;
- restart/reconnect com nova connectionGeneration: PASS;
- DummyCopy, Neural 1.0, N/N-1 e escala reduzida 0.5: PASS;
- nenhum wait infinito ou GetOverlappedResult(..., TRUE) no caminho Capture/Host revisado.

Código validado: `f57e6d735b78c834e37b09815aee3ab2af1d4143`.
